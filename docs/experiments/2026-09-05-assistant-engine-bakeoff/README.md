# Assistant engine bake-off, 2026-09-05

The record behind desk ADR-0001 (`docs/adr/0001-make-the-assistant-engine-a-slot.md`).

- `EXPERIMENT.md` — the design, frozen before any candidate was built, with the amendment
  that added the thinking-and-critical-mode criterion and the note recording which Go
  framework was actually built.
- `DECISION.md` — the final memo: the recommendation, the kill-criteria and score tables with
  and without the added criterion, what was measured per candidate, the dissent, and the open
  gaps ranked by how much they could change the decision.

The per-candidate prototypes, the scripted-model fixture, the verifier and judge reports, and
the run logs the memo cites are not carried here; they were held in a working directory at
the time of drafting, and the memo's file paths refer to that layout. Every number in the ADR
traces to `DECISION.md`.
